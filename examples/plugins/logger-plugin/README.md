# Logger plugin

Ejemplo sin dependencias que recibe y verifica webhooks HMAC de TrastCMS.

```bash
TRASTCMS_PLUGIN_SECRET=replace-with-the-same-16-character-secret node server.mjs
```

Después registre `http://localhost:9090` desde **Administración → Plugins**.
